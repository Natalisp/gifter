<% if @users.blank? %>
  <h4>There are no recipes containing the term <%= params[:search] %>.</h4>
  <% else %>
  <% @users.each do |user| %>
  <p>
  <%= link_to user.first_name, user_path(user) %>
  </p>
  <% end %>
<% end %>
